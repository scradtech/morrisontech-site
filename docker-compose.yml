services:
  morrisontech-site:
    build: .
    container_name: morrisontech-site
    restart: unless-stopped
    ports:
      - "8881:80"
    networks:
      - proxy
    labels:
      - "VIRTUAL_HOST=morrisontech.com.au"
      - "VIRTUAL_PORT=80"

networks:
  proxy:
    external: true
